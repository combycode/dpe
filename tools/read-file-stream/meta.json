{
  "name": "read-file-stream",
  "version": "0.1.0",
  "description": "Streams rows from a file (NDJSON / lines / CSV). One stdin envelope per file; one stdout envelope per row.",
  "runtime": "rust",
  "entry": "target/release/read-file-stream",
  "run": "cargo run --release --",
  "build": "cargo build --release",
  "test": "cargo test",
  "test_skipped": true,
  "test_skipped_reason": "Pure file-to-row streaming; format parsing fully covered by tests/. Stage-level harness adds no coverage beyond cargo tests."
}

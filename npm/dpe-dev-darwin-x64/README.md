# @combycode/dpe-dev-darwin-x64

Placeholder for the darwin-x64 native build of `dpe-dev`.

Users should not install this directly — install `dpe-dev` from npm and the right platform package will be pulled automatically via `optionalDependencies`.

Real binaries are published on the first stable release tag.

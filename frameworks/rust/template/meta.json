{
  "name": "{{tool_name_kebab}}",
  "version": "0.1.0",
  "description": "{{description}}",
  "runtime": "rust",
  "entry": "target/release/{{tool_name_kebab}}",
  "run": "cargo run --release --",
  "build": "cargo build --release",
  "test": "cargo test"
}
